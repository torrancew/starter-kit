# == Class: <%= module_name %>
#
# <%= description || summary %>
#
# === Parameters:
#
# None
#
# === Variables:
#
# None
#
# === Examples:
#
# None
#
# === Authors:
#
# * <%= author || username %>
#
# === Copyright:
#
# Copyright <%= Time.now.year %>, <%= author || username %>
# unless otherwise noted.
#
class <%= module_name %> {
  class {
    '<%= module_name %>::install':
      ensure => present;
    '<%= module_name %>::configure':
      ensure => present;
    '<%= module_name %>::service':
      ensure => present;
  }

  anchor { '<%= module_name %>::begin': } -> Class['<%= module_name %>::install']
  Class['<%= module_name %>::install']    -> anchor { '<%= module_name %>::end': }
}

